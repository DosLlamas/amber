import React, { useState } from "react";
import logo from "./assets/gadget2.png";
const url = chrome.runtime.getURL(logo);

function Time({ setShow, handleTimer, setHandleTimer }) {
 
  return (
    <div className="timeframe">
        
    </div>
  );
}

export default Time;
